{
  "name": "incubxperts-website",
  "private": true,
  "version": "1.0.0",
  "description": "IncubXperts corporate website — static HTML/CSS/JS with a small build script for case-study pages.",
  "scripts": {
    "build:pages": "node scripts/build-pages.js",
    "build:case-studies": "node scripts/build-case-studies.js",
    "build:services": "node scripts/build-services.js",
    "build": "npm run build:pages && npm run build:case-studies && npm run build:services",
    "style-diff": "node scripts/style-diff.js",
    "style-diff:fast": "node scripts/style-diff.js --fast",
    "postinstall": "node scripts/install-hooks.js"
  },
  "dependencies": {
    "marked": "12.0.2"
  },
  "devDependencies": {
    "puppeteer-core": "23.11.1"
  }
}
